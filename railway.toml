# Property Maintenance Dashboard - Combined Backend + Frontend Deployment
# Python backend with pre-built React frontend assets

[build]
builder = "NIXPACKS"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
numReplicas = 1

[env]
PYTHON_VERSION = "3.11"
NODE_ENV = "production"
